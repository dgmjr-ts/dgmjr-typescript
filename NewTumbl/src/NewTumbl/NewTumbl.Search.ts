import { NewTumblLogin } from "./NewTumbl.Login";

export class NewTumblSearch extends NewTumblLogin {


    public async search() {
        var aParams = age.bnSession, Page.bnTab, Session.dwBlogIx, Session.Blog ?Session.Blog.dwBlogIx: 0, this.dtSearch, this.nPage, this.nPerPage, this.nOffset];
    }
}
